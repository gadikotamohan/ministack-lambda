name: Deploy Kubectl Lambda to MiniStack

on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    services:
      ministack:
        image: nahuelnucera/ministack
        ports:
          - 4566:4566
        env:
          LOG_LEVEL: INFO

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.5.0"

      - name: Download and Verify kubectl
        run: |
          KUBECTL_VERSION=$(curl -L -s https://dl.k8s.io/release/stable.txt)
          echo "Downloading kubectl version ${KUBECTL_VERSION}"
          
          # Download binary
          curl -LO "https://dl.k8s.io/release/${KUBECTL_VERSION}/bin/linux/amd64/kubectl"
          
          # Download checksum
          curl -LO "https://dl.k8s.io/release/${KUBECTL_VERSION}/bin/linux/amd64/kubectl.sha256"
          
          # Verify checksum
          echo "$(cat kubectl.sha256)  kubectl" | sha256sum --check
          
          chmod +x kubectl

      - name: Package Lambda function
        run: |
          zip lambda_function_payload.zip handler.py kubectl

      - name: Terraform Init
        run: terraform init

      - name: Terraform Apply
        run: terraform apply -auto-approve

      - name: Diagnostics and Test
        run: |
          export AWS_ACCESS_KEY_ID=test
          export AWS_SECRET_ACCESS_KEY=test
          export AWS_DEFAULT_REGION=us-east-1
          
          echo "Listing Lambda Functions..."
          aws --endpoint-url=http://localhost:4566 lambda list-functions
          
          echo "Inspecting Function URL Config..."
          # This is the most important diagnostic to see what MiniStack thinks the URL is
          aws --endpoint-url=http://localhost:4566 lambda get-function-url-config --function-name kubectl-version-lambda > url_config.json
          cat url_config.json
          
          # Extract the URL from the config itself
          REAL_URL=$(jq -r '.FunctionUrl' url_config.json)
          echo "Real MiniStack URL: $REAL_URL"
          
          URL_ID=$(echo "$REAL_URL" | sed -e 's|^[^/]*//||' -e 's/\..*//')
          echo "Real URL ID: $URL_ID"
          
          # Try variations including the port and different ID sources
          FORMATS=(
            "$URL_ID.lambda-url.localhost"
            "$URL_ID.lambda-url.localhost:4566"
            "kubectl-version-lambda.lambda-url.localhost"
            "kubectl-version-lambda.lambda-url.localhost:4566"
            "kubectl-version-lambda.localhost"
            "kubectl-version-lambda.localhost:4566"
          )
          
          for i in {1..5}; do
            echo "Attempt $i..."
            
            # Diagnostic: Try calling the function via its internal MiniStack path if known
            # MiniStack sometimes exposes services at /2015-03-31/functions/...
            echo "Testing Direct API Invoke path..."
            curl -s -X POST "http://localhost:4566/2015-03-31/functions/kubectl-version-lambda/invocations" -d '{}' | jq . || echo "Direct path failed"

            for HOST in "${FORMATS[@]}"; do
              echo "Testing Host: $HOST"
              RESPONSE=$(curl -s -H "Host: $HOST" http://localhost:4566)
              echo "Response for $HOST: $RESPONSE"
              
              if [[ "$RESPONSE" == *"clientVersion"* ]]; then
                 echo "Success with Host: $HOST"
                 echo "$RESPONSE" | jq .
                 exit 0
              fi
            done
            sleep 10
          done
                 echo "$RESPONSE" | jq .
                 exit 0
              fi
            done
            sleep 10
          done
          echo "Failed to find a working Host header for Function URL"
          exit 1
